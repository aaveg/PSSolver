from setuptools import setup, find_packages

setup(
    name='pssolver',              # Required: name of the package
    version='0.1',                # Required: version number
    packages=find_packages(),     # Required: automatically find all modules inside the folder
)